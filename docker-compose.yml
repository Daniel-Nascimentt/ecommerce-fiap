version: '3.8'

services:
  postgres:
    image: postgres:latest
    container_name: meu-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: ms_usuarios,ms_carrinho,ms_gt_itens
    command: >
      bash -c '
        echo "CREATE DATABASE ms_usuarios;" >> /docker-entrypoint-initdb.d/init.sql &&
        echo "CREATE DATABASE ms_carrinho;" >> /docker-entrypoint-initdb.d/init.sql &&
        echo "CREATE DATABASE ms_gt_itens;" >> /docker-entrypoint-initdb.d/init.sql &&
        docker-entrypoint.sh postgres
      ' 
    ports:
      - "5432:5432"